spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://mysql:3306/channels_db?serverTimezone=UTC}
    username: ${DB_USERNAME:app}
    password: ${DB_PASSWORD:app}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP:kafka:9092}
